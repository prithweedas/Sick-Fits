# import * from './generated/prisma.graphql'

type Mutation {
  createItem(data: CreateItemInput!): Item!
}

type Query {
  items: [Item!]!
}

input CreateItemInput {
  title: String!
  description: String!
  image: String
  largeImage: String
  price: Int!
}
